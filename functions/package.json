{
  "name": "estacionamientos-functions",
  "version": "1.0.0",
  "private": true,
  "description": "Cloud Functions de facturación CFDI para estacionamientos-app — backend del portal de autofacturación.",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "npm run build && firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "dependencies": {
    "facturapi": "^4.16.0",
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.3.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
